{{!< default}}

<header class="page-header">
  {{#author}}
    <h1>{{name}}</h1>
    {{#if bio}}<p>{{bio}}</p>{{/if}}
  {{/author}}
</header>

<div class="post-feed">
  {{#foreach posts}}
    {{> post-card}}
  {{/foreach}}
  {{pagination}}
</div>
